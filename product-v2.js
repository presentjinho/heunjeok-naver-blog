(function(){'use strict';
  const B=globalThis.HeunjeokBackup||{exportData(){throw new Error('backup unavailable')},parse(){return{ok:false}},importData(){return{ok:false}}};const d=document,$=id=>d.getElementById(id),q=s=>d.querySelector(s),qa=s=>[...d.querySelectorAll(s)];
  const make=(tag,attrs={},text='')=>{const el=d.createElement(tag);for(const [k,v] of Object.entries(attrs)){if(k==='class')el.className=v;else if(k==='dataset')Object.assign(el.dataset,v);else el.setAttribute(k,v)}if(text)el.textContent=text;return el};
  const prefKey='heunjeok-ui-preferences';const readPrefs=()=>{try{return JSON.parse(localStorage.getItem(prefKey)||'{}')}catch{return{}}};const savePrefs=patch=>{try{localStorage.setItem(prefKey,JSON.stringify({...readPrefs(),...patch,updatedAt:new Date().toISOString()}))}catch{}};
  let questionIndex=0,lastInputSnapshot='';
  if(globalThis.HTMLDialogElement){if(!HTMLDialogElement.prototype.showModal)HTMLDialogElement.prototype.showModal=function(){this.setAttribute('open','')};if(!HTMLDialogElement.prototype.close)HTMLDialogElement.prototype.close=function(){this.removeAttribute('open')}}
  function runtimeKind(){if(location.protocol==='file:')return'file';if(/\.github\.io$/i.test(location.hostname)||globalThis.__HEUNJEOK_RUNTIME__==='pages')return'pages';return'server'}
  function runtimeBanner(){const kind=runtimeKind();d.documentElement.dataset.runtime=kind;const badge=q('.local-badge');badge.textContent=kind==='server'?'서버 연결판 · 공식 데이터 기능은 로그인 후':'공개 로컬판 · 데이터는 이 브라우저만 · 로그인 없음'}
  function position(){q('.kicker').textContent='네이버 블로그 발행 준비 작업실';$('hero-title').textContent='내 경험을 네이버 블로그 글로 정리해요.';q('.hero-copy').textContent='주제 선택부터 경험 정리, 교정, 제목·본문·태그 복사까지 네이버 블로그에 필요한 준비만 한곳에서 합니다.';const note=make('p',{class:'hero-position'},'네이버 로그인이나 자동 발행 권한을 받지 않습니다. 완성한 글을 일반 텍스트로 복사한 뒤 네이버에서 직접 확인하고 발행합니다.');q('.hero').append(note)}
  function quickRoute(){const box=make('section',{class:'quick-route','aria-label':'첫 글 최소 경로'});box.append(make('strong',{},'90초 최소 경로'));const ol=make('ol');['주제 1개','경험 3칸','초안','검수','복사'].forEach(v=>ol.append(make('li',{},v)));box.append(ol);const actions=make('div',{class:'quick-route-actions'});const demo=make('button',{type:'button',class:'secondary',id:'fillDemo'},'데모 바로 만들기');const help=make('button',{type:'button',class:'text-button',id:'showOnboarding'},'처음 사용법');const backup=make('a',{class:'text-button',href:'#data-title'},'백업·기기 이전');actions.append(demo,help,backup);box.append(actions);const prefs=readPrefs();if(!prefs.lastBackupAt)box.append(make('p',{class:'backup-reminder'},'아직 백업이 없습니다. 기기를 바꾸기 전 JSON 백업을 저장하세요. 사진은 포함되지 않습니다.'));q('.hero').insertAdjacentElement('afterend',box);demo.addEventListener('click',fillDemo);help.addEventListener('click',()=>{q('.onboarding-dialog')?.remove();savePrefs({onboarded:false});onboarding()})}
  function simplifyFlow(){const input=q('.input-column'),tone=$('tone-tool'),experience=$('experience-tool');if(!input||!tone||!experience)return;input.insertBefore(experience,tone);experience.querySelector('.card-heading>span').textContent='02';const details=make('details',{class:'optional-tone'});details.append(make('summary',{},'내 말투도 반영하기 (선택)'));tone.parentElement.insertBefore(details,tone);details.append(tone);tone.querySelector('.card-heading>span').textContent='선택';const resultNumber=q('#resultPanel .section-number');if(resultNumber)resultNumber.textContent='03'}
  function topicChosen(){return !/^주제를 (하나 골라|먼저)/.test($('selectedTopicStatus')?.textContent||'')}
  function essentialFields(){return qa('#experienceFields textarea')}
  function progress(){const count=essentialFields().filter(el=>el.value.trim().replace(/\s/g,'').length>=5).length;const generated=!$('resultContent')?.hidden;const steps=[topicChosen(),count===3,generated,generated,generated&&$('copyBody')?.getAttribute('aria-disabled')!=='true'];const labels=[`주제${steps[0]?' ✓':''}`,`경험 ${count}/3`,`초안 ${generated?'✓':'미생성'}`,`검수`,`복사 ${steps[4]?'가능':'잠김'}`];qa('.wizard-nav button').forEach((button,i)=>{button.classList.toggle('is-done',steps[i]);button.querySelector('small').textContent=labels[i]});const completed=steps.filter(Boolean).length;q('.progress-live span').style.width=`${completed*20}%`;updateGenerateReason();return{count,generated,steps}}
  function wizard(){const nav=make('nav',{class:'wizard-nav','aria-label':'글 작성 단계'});const targets=['topicSection','experience-tool','resultPanel','quality-tool','publish-tool'];['1 주제','2 경험','3 초안','4 검수','5 복사'].forEach((label,i)=>{const b=make('button',{type:'button'},label);b.append(make('small',{},''));b.addEventListener('click',()=>$(targets[i])?.scrollIntoView({behavior:'smooth',block:'start'}));nav.append(b)});const bar=make('div',{class:'progress-live','aria-hidden':'true'});bar.append(make('span'));nav.append(bar);q('.quick-route').insertAdjacentElement('afterend',nav);if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!visible)return;const i=targets.indexOf(visible.target.id);qa('.wizard-nav button').forEach((b,n)=>b.classList.toggle('is-current',n===i))},{rootMargin:'-15% 0px -65%',threshold:[0,.1,.5]});targets.forEach(id=>$(id)&&observer.observe($(id)))}const stateObserver=new MutationObserver(()=>{if(q('.progress-live span'))progress()});[$('selectedTopicStatus'),$('resultContent'),$('copyLockReason')].filter(Boolean).forEach(el=>stateObserver.observe(el,{attributes:true,childList:true,characterData:true,subtree:true}))}
  function mobileTabs(){const nav=make('nav',{class:'mobile-tabs','aria-label':'모바일 화면 전환'});[['입력','experience-tool'],['결과','resultPanel'],['도구','quality-tool']].forEach(([label,id],i)=>{const b=make('button',{type:'button'},label);b.addEventListener('click',()=>{$(id)?.scrollIntoView({behavior:'smooth',block:'start'});qa('.mobile-tabs button').forEach((v,n)=>v.classList.toggle('is-active',n===i))});nav.append(b)});d.body.append(nav)}
  function advanced(){const sections=[q('[aria-labelledby="comment-title"]'),$('live-research'),q('[aria-labelledby="benchmark-title"]'),q('[aria-labelledby="postlog-title"]')].filter(Boolean);if(!sections.length)return;const details=make('details',{class:'advanced-tools'});const summary=make('summary',{},'발행 후·참고 도구 더 보기');const body=make('div',{class:'advanced-tools-body'});details.append(summary,body);sections[0].parentElement.insertBefore(details,sections[0]);sections.forEach(section=>body.append(section));q('.live-category-grid')?.removeAttribute('style')}
  function onboarding(){if(readPrefs().onboarded)return;const dialog=make('dialog',{class:'onboarding-dialog','aria-labelledby':'onboardV2Title'});const title=make('h2',{id:'onboardV2Title'},'첫 글은 세 단계면 됩니다');const list=make('ol');['분야와 주제를 고릅니다.','실제로 겪은 일 세 칸을 답합니다.','초안을 만들고 검수한 뒤 복사합니다.'].forEach(v=>list.append(make('li',{},v)));const actions=make('div',{class:'action-row'});const demo=make('button',{type:'button',class:'primary'},'데모로 먼저 보기');const start=make('button',{type:'button',class:'secondary'},'내 글 시작');actions.append(demo,start);dialog.append(title,list,make('p',{},'말투 카드는 선택입니다. 등록하지 않아도 바로 만들 수 있어요.'),actions);d.body.append(dialog);demo.addEventListener('click',()=>{savePrefs({onboarded:true});dialog.close();fillDemo()});start.addEventListener('click',()=>{savePrefs({onboarded:true});dialog.close();$('topicSection')?.scrollIntoView()});dialog.addEventListener('cancel',()=>savePrefs({onboarded:true}));dialog.showModal()}
  function fillDemo(){const category=$('category');category.value='local';category.dispatchEvent(new Event('change',{bubbles:true}));const type=$('postType');type.value='visit';type.dispatchEvent(new Event('change',{bubbles:true}));$('customTopic').value='성수동 노트북 작업 카페 방문 기록';$('useCustomTopic').click();setTimeout(()=>{const values=['지난 토요일 오후 2시에 성수동 카페에 혼자 갔어요.','노트북으로 두 시간 작업할 조용한 자리가 필요했어요.','창가 자리는 밝았지만 오후 4시부터 대화 소리가 커져 아쉬웠어요.'];essentialFields().forEach((field,i)=>{field.value=values[i]||'';field.dispatchEvent(new Event('input',{bubbles:true}))});$('relationship').value='selfpaid';$('relationship').dispatchEvent(new Event('change',{bubbles:true}));progress();$('generate').click();showCopyStatus('데모 초안입니다. 숫자와 경험은 예시이므로 내 내용으로 바꿔주세요.',false)},30)}
  function updateGenerateReason(){const button=$('generate');if(!button)return;let reason=$('generateReason');if(!reason){reason=make('p',{id:'generateReason',class:'field-hint','aria-live':'polite'});button.insertAdjacentElement('afterend',reason);button.setAttribute('aria-describedby','generateReason')}const fields=globalThis.HeunjeokCore.getInterviewFields($('postType').value).filter(field=>field.essential);const answers={};fields.forEach((field,index)=>answers[field.key]=essentialFields()[index]?.value||'');const gate=globalThis.HeunjeokCore.canGenerate({topic:topicChosen()?$('selectedTopicStatus').textContent:'',answers,memo:$('memo')?.value||'',postType:$('postType').value});reason.textContent=gate.ok?'말투 카드 없이도 만들 수 있어요. '+gate.reason:gate.reason;button.disabled=!gate.ok;essentialFields().forEach((field,i)=>{const invalid=gate.missing.includes(fields[i]?.key)&&!gate.memoReady;const host=field.closest('.experience-field')||field.parentElement;host.classList.toggle('is-invalid',invalid);let hint=host.querySelector('.field-hint');if(!hint){hint=make('p',{class:'field-hint'});field.insertAdjacentElement('afterend',hint)}hint.textContent=invalid?'다섯 글자 이상으로 실제 상황을 적어주세요.':''})}
  function interviewTools(){const host=$('experienceFields');const controls=make('div',{class:'one-question-controls'});const toggle=make('button',{type:'button',class:'secondary'},'한 질문씩 보기');const prev=make('button',{type:'button',class:'secondary'},'이전');const next=make('button',{type:'button',class:'secondary'},'다음');controls.append(toggle,prev,next);host.insertAdjacentElement('beforebegin',controls);function render(){const fields=qa('#experienceFields .experience-field');fields.forEach((v,i)=>v.classList.toggle('is-question-current',i===questionIndex));prev.disabled=questionIndex===0;next.disabled=questionIndex>=fields.length-1}toggle.addEventListener('click',()=>{const on=$('experience-tool').classList.toggle('one-question-mode');toggle.textContent=on?'전체 질문 보기':'한 질문씩 보기';render()});prev.addEventListener('click',()=>{questionIndex=Math.max(0,questionIndex-1);render()});next.addEventListener('click',()=>{questionIndex=Math.min(essentialFields().length-1,questionIndex+1);render()});if('SpeechRecognition'in window||'webkitSpeechRecognition'in window){essentialFields().forEach(field=>{const b=make('button',{type:'button',class:'text-button voice-button'},'🎙 음성으로 답하기');b.addEventListener('click',()=>speech(field,b));field.insertAdjacentElement('afterend',b)})}const favorite=make('div',{class:'inline-action'});const input=make('input',{id:'favoritePhrase',placeholder:'자주 쓰는 장소·제품명'});const add=make('button',{type:'button',class:'secondary'},'즐겨찾기 저장');favorite.append(input,add);host.insertAdjacentElement('afterend',favorite);add.addEventListener('click',()=>{const value=input.value.trim();if(!value)return;const prefs=readPrefs();savePrefs({favorites:[...new Set([...(prefs.favorites||[]),value])].slice(0,8)});input.value='';renderFavorites(favorite)});renderFavorites(favorite);host.addEventListener('input',progress)}
  function renderFavorites(anchor){anchor.parentElement.querySelector('.favorite-chips')?.remove();const box=make('div',{class:'tone-chips favorite-chips'});(readPrefs().favorites||[]).forEach(value=>{const b=make('button',{type:'button',class:'secondary'},value);b.addEventListener('click',()=>{const field=essentialFields()[0];field.value=[field.value,value].filter(Boolean).join(' ');field.dispatchEvent(new Event('input',{bubbles:true}))});box.append(b)});anchor.insertAdjacentElement('afterend',box)}
  function speech(field,button){const R=window.SpeechRecognition||window.webkitSpeechRecognition;const rec=new R();rec.lang='ko-KR';rec.interimResults=false;button.textContent='듣는 중…';rec.onresult=e=>{field.value=[field.value,e.results[0][0].transcript].filter(Boolean).join(' ');field.dispatchEvent(new Event('input',{bubbles:true}))};rec.onerror=()=>button.textContent='음성 입력 실패';rec.onend=()=>{if(button.textContent==='듣는 중…')button.textContent='🎙 음성으로 답하기'};rec.start()}
  function toneTools(){const area=$('toneSamples'),profile=$('toneProfile');const meter=make('div',{class:'tone-meter',role:'progressbar','aria-label':'말투 표본 안정도','aria-valuemin':'0','aria-valuemax':'500','aria-valuenow':'0'});meter.append(make('span'));const label=make('p',{class:'storage-note'},'표본 약함 · 200~500자 권장');area.insertAdjacentElement('afterend',meter);meter.insertAdjacentElement('afterend',label);area.addEventListener('input',()=>{const n=area.value.trim().length;meter.firstChild.style.width=`${Math.min(100,n/5)}%`;meter.setAttribute('aria-valuenow',String(Math.min(500,n)));label.textContent=n<200?`표본 약함 · ${n}자`:n<400?`표본 보통 · ${n}자`:`표본 안정 · ${n}자`});const row=make('div',{class:'tone-strength'});row.append(make('label',{for:'toneStrength'},'적용 강도'));const select=make('select',{id:'toneStrength'});[['weak','약 · 연결어만'],['medium','중 · 문장 호흡'],['strong','강 · 말끝 참고']].forEach(([v,t])=>select.append(make('option',{value:v},t)));row.append(select);profile.insertAdjacentElement('beforebegin',row);select.value=readPrefs().toneStrength||'weak';select.addEventListener('change',()=>savePrefs({toneStrength:select.value}));const profiles=make('div',{class:'v2-actions'});const name=make('input',{placeholder:'카드 이름: 일상/리뷰/공지'});const save=make('button',{type:'button',class:'secondary'},'현재 카드 프로필 저장');const picker=make('select',{'aria-label':'말투 프로필 전환'});profiles.append(name,save,picker);profile.insertAdjacentElement('afterend',profiles);function render(){picker.replaceChildren(make('option',{value:''},'저장한 말투 카드 전환'));const all=readPrefs().toneProfiles||{};Object.keys(all).forEach(key=>picker.append(make('option',{value:key},key)))}save.addEventListener('click',()=>{const key=name.value.trim();const current=localStorage.getItem('heunjeok-naver-tone');if(!key||!current)return;const all=readPrefs().toneProfiles||{};all[key]=current;savePrefs({toneProfiles:all});name.value='';render()});picker.addEventListener('change',()=>{const value=(readPrefs().toneProfiles||{})[picker.value];if(value&&confirm('선택한 말투 카드로 바꾸고 화면을 새로 열까요?')){localStorage.setItem('heunjeok-naver-tone',value);location.reload()}});render()}
  function editor(){const draft=$('draft');const meta=make('div',{class:'editor-meta'});const stats=make('span');const toggle=make('div',{class:'result-view-toggle'});const edit=make('button',{type:'button','aria-pressed':'true'},'편집');const preview=make('button',{type:'button','aria-pressed':'false'},'네이버 미리보기');const full=make('button',{type:'button','aria-pressed':'false'},'결과 크게 보기');toggle.append(edit,preview,full);draft.insertAdjacentElement('beforebegin',toggle);draft.insertAdjacentElement('afterend',meta);meta.append(stats,make('span',{},'문단을 늘리기보다 확인한 경험을 더 적어주세요.'));const mobile=q('.mobile-preview');function update(){const value=draft.value.trim(),chars=value.replace(/\s/g,'').length,minutes=chars?Math.max(1,Math.ceil(chars/500)):0,paragraphs=value?value.split(/\n\s*\n+/).length:0;stats.textContent=`공백 제외 ${chars.toLocaleString()}자 · 약 ${minutes}분 · ${paragraphs}문단`}$('resultPanel').addEventListener('input',update);edit.addEventListener('click',()=>{draft.hidden=false;mobile.hidden=true;edit.setAttribute('aria-pressed','true');preview.setAttribute('aria-pressed','false')});preview.addEventListener('click',()=>{draft.hidden=true;mobile.hidden=false;mobile.open=true;edit.setAttribute('aria-pressed','false');preview.setAttribute('aria-pressed','true')});full.addEventListener('click',()=>{const on=$('resultPanel').classList.toggle('is-fullscreen');full.setAttribute('aria-pressed',String(on));full.textContent=on?'크게 보기 닫기':'결과 크게 보기'});$('staleNotice').append(make('button',{type:'button',class:'secondary',id:'regenerateNow'},'변경 내용으로 다시 만들기'));$('regenerateNow').addEventListener('click',()=>$('generate').click());$('titleCandidates').addEventListener('click',e=>{const item=e.target.closest('li');if(!item||e.target.tagName==='BUTTON')return;const title=item.querySelector('span')?.childNodes[0]?.textContent?.trim();if(title&&confirm('선택한 제목을 본문 첫 줄에 넣을까요?')){draft.value=`${title}\n\n${draft.value.replace(/^.*\n\n/,'')}`;draft.dispatchEvent(new Event('input',{bubbles:true}))}});update()}
  function naverFinishPanel(){const host=make('section',{class:'naver-finish-panel','aria-labelledby':'naverFinishTitle'});host.append(make('h3',{id:'naverFinishTitle'},'네이버에 옮길 준비'));const steps=make('ol',{class:'naver-handoff'});['제목을 붙여넣습니다.','본문을 붙여넣습니다.','사진·장소·공개 범위·광고 고지는 네이버에서 확인합니다.'].forEach(text=>steps.append(make('li',{},text)));const preview=make('details',{class:'copy-format-preview'});preview.append(make('summary',{},'제목·본문·태그 모두 복사 형식'));preview.append(make('pre',{},'제목\n\n본문\n\n#태그'));const link=make('a',{class:'naver-link',href:'https://blog.naver.com/',target:'_blank',rel:'noopener noreferrer'},'네이버 블로그 열기 ↗');host.append(steps,preview,make('p',{class:'storage-note'},'글쓰기 진입 화면은 네이버 로그인 상태와 기기에 따라 달라질 수 있어요.'),link);q('.publish-actions').insertAdjacentElement('beforebegin',host)}
  function copyStatus(){const p=make('p',{id:'copyStatusAlways',class:'copy-status-always','aria-live':'polite'},'검색 노출·순위를 보장하지 않습니다. 복사 잠금 사유가 있으면 여기에 표시됩니다.');q('.publish-actions').insertAdjacentElement('beforebegin',p);const sync=()=>{const target=$('copyLockReason');if(!target)return;const reason=target.textContent.trim();if(reason)showCopyStatus(reason,true)};new MutationObserver(sync).observe($('copyLockReason'),{childList:true,characterData:true,subtree:true});sync();qa('.publish-actions button').forEach(button=>button.addEventListener('click',()=>setTimeout(()=>{if(button.getAttribute('aria-disabled')!=='true')showCopyStatus('복사 완료. 다음: 네이버를 열어 숫자·고지·문장 흐름을 확인하세요.',false)},50)))}
  function showCopyStatus(text,locked){const el=$('copyStatusAlways');el.textContent=text;el.classList.toggle('is-locked',locked)}
  function quality(){const list=$('qualityList');list.addEventListener('click',e=>{const item=e.target.closest('li');if(!item)return;const quote=item.querySelector('q,code')?.textContent.trim();const draft=$('draft');if(quote){const index=draft.value.indexOf(quote.slice(0,30));if(index>=0){draft.focus();draft.setSelectionRange(index,index+quote.length);draft.classList.add('focus-flash');setTimeout(()=>draft.classList.remove('focus-flash'),1300)}}});const preview=$('correctionPreview');const compare=make('div',{class:'compare-preview'});const before=make('pre',{},'검수 전 본문');const after=make('pre',{},'안전 교정본');compare.append(before,after);preview.parentElement.insertBefore(compare,preview);$('runQuality').addEventListener('click',()=>setTimeout(()=>{before.textContent=$('draft').value;after.textContent=preview.value;const items=qa('#qualityList li');const severity={high:0,medium:1,low:2};items.sort((a,b)=>(severity[a.dataset.severity]??1)-(severity[b.dataset.severity]??1)).forEach(item=>list.append(item))},0))}
  function backup(){const section=q('[aria-labelledby="data-title"]');const panel=make('div',{class:'backup-panel'});panel.append(make('h3',{},'다른 기기로 옮기기'));const note=make('p',{class:'storage-note'},'백업 파일에는 경험 입력·말투 카드·초안·버전·발행 기록이 들어갑니다. 사진은 포함되지 않습니다.');const file=make('input',{type:'file',id:'importBackup',accept:'application/json'});const fileLabel=make('label',{for:'importBackup'},'흔적 JSON 백업 파일');const modeSelect=make('select',{id:'importMode','aria-label':'가져오기 방식'});modeSelect.append(make('option',{value:'merge'},'기존 내용 유지하고 빈 항목만 합치기'),make('option',{value:'replace'},'현재 데이터를 지우고 백업으로 교체'));const button=make('button',{type:'button',class:'primary'},'백업 가져오기');const status=make('p',{role:'status',class:'storage-note'});panel.append(note,fileLabel,file,modeSelect,button,status,make('p',{class:'storage-note'},'새 기기에서 이 페이지를 열고 백업 파일을 선택하면 됩니다. 시크릿 모드에서는 창을 닫을 때 저장 내용이 사라질 수 있습니다.'));section.append(panel);$('exportLocalData').addEventListener('click',e=>{e.preventDefault();try{const payload=B.exportData(localStorage);const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'});const a=make('a',{download:`heunjeok-backup-v2-${new Date().toISOString().slice(0,10)}.json`});a.href=URL.createObjectURL(blob);a.click();URL.revokeObjectURL(a.href);savePrefs({lastBackupAt:new Date().toISOString()});status.textContent='백업 파일을 만들었습니다. 본인 기기에 보관해 주세요.'}catch{status.textContent='브라우저 저장소를 읽지 못해 백업하지 못했습니다. 시크릿 모드인지 확인해 주세요.'}},true);button.addEventListener('click',async()=>{if(!file.files[0]){status.textContent='백업 파일을 선택해 주세요.';return}let text;try{text=await file.files[0].text()}catch{status.textContent='파일을 읽지 못했습니다. 다시 선택해 주세요.';return}const parsed=B.parse(text);if(!parsed.ok){status.textContent='흔적 백업 파일 형식이 아니거나 손상되었습니다.';return}if(!confirm(modeSelect.value==='replace'?'현재 저장 내용을 지우고 백업으로 바꿀까요?':'비어 있는 항목에 백업 내용을 합칠까요?'))return;const result=B.importData(localStorage,text,modeSelect.value);if(!result.ok){status.textContent='저장 공간이 부족하거나 차단되어 가져오지 못했습니다. 기존 내용은 복구했습니다.';return}status.textContent=`${result.count}개 항목을 가져왔습니다. 화면을 새로 엽니다.`;setTimeout(()=>location.reload(),600)});storageInfo(section);const last=readPrefs().lastBackupAt;if(!last||Date.now()-Date.parse(last)>7*864e5)status.textContent='마지막 백업이 7일 넘었거나 없습니다. 기기 이전 전에 백업을 권장합니다.'}
  async function storageInfo(section){let bytes=0;for(let i=0;i<localStorage.length;i++){const key=localStorage.key(i);bytes+=(key.length+(localStorage.getItem(key)||'').length)*2}let text=`브라우저 글 데이터 약 ${(bytes/1024).toFixed(1)}KB`;try{const estimate=await navigator.storage?.estimate?.();if(estimate?.usage)text+=` · 사진 포함 전체 약 ${(estimate.usage/1024/1024).toFixed(1)}MB`}catch{}section.insertBefore(make('p',{class:'storage-note'},text),section.children[1]||null)}
  function theme(){const button=make('button',{type:'button',class:'theme-toggle','aria-label':'밝은 화면과 어두운 화면 전환'},'◐');d.body.append(button);const saved=readPrefs().theme||'light';d.documentElement.dataset.theme=saved;button.addEventListener('click',()=>{const next=d.documentElement.dataset.theme==='dark'?'light':'dark';d.documentElement.dataset.theme=next;document.querySelector('meta[name="theme-color"]').content=next==='dark'?'#101813':'#0f5132';savePrefs({theme:next})})}
  function footer(){const footer=make('footer',{class:'site-footer'});footer.append(make('strong',{},'흔적은 네이버 공식 서비스가 아닙니다. '));const guide=make('a',{href:'./guide.html'},'사용 방법');const privacy=make('a',{href:'./privacy.html'},'개인정보 요약');const security=make('a',{href:'./security.html'},'보안 원칙');const build=document.querySelector('meta[name="heunjeok-build"]')?.content||'dev';const buildText=make('span',{class:'build-info'},`공개판 build ${build}`);const copy=make('button',{type:'button',class:'text-button build-copy'},'빌드 정보 복사');copy.addEventListener('click',async()=>{const value=`흔적 공개판 build ${build}`;try{await navigator.clipboard.writeText(value);copy.textContent='복사됨'}catch{copy.textContent=value}});footer.append(guide,d.createTextNode(' · '),privacy,d.createTextNode(' · '),security,d.createTextNode(' · '),buildText,copy,make('p',{},'자동 로그인·자동 발행·가짜 조회수·입력 없는 경험 생성 없음'));q('main').append(footer)}
  function hashtagEditor(){const source=$('hashtags');const box=make('div',{class:'hashtag-editor','aria-label':'해시태그 편집'});const row=make('div',{class:'inline-action'});const input=make('input',{placeholder:'태그 추가(최대 3개)','aria-label':'해시태그 추가'});const add=make('button',{type:'button',class:'secondary'},'추가');row.append(input,add);source.insertAdjacentElement('afterend',box);box.insertAdjacentElement('afterend',row);function tags(){return source.textContent.match(/#[0-9A-Za-z가-힣_]+/g)||[]}function render(){box.replaceChildren();tags().forEach(tag=>{const chip=make('span',{class:'hashtag-chip'},tag);const remove=make('button',{type:'button','aria-label':`${tag} 삭제`},'×');remove.addEventListener('click',()=>{source.textContent=tags().filter(v=>v!==tag).join(' ');render()});chip.append(remove);box.append(chip)})}add.addEventListener('click',()=>{let value=input.value.trim().replace(/^#+/,'').replace(/[^0-9A-Za-z가-힣_]/g,'');const current=tags();if(!value||current.length>=3)return;source.textContent=[...current,`#${value}`].join(' ');input.value='';render()});new MutationObserver(render).observe(source,{childList:true,characterData:true,subtree:true});render()}
  function provenanceLinks(){const draft=$('draft');$('provenanceList').addEventListener('click',event=>{const item=event.target.closest('li');if(!item)return;const evidence=item.textContent.split(':').slice(1).join(':').trim().slice(0,45);const index=draft.value.indexOf(evidence);if(index>=0){draft.focus();draft.setSelectionRange(index,index+evidence.length);draft.classList.add('focus-flash');setTimeout(()=>draft.classList.remove('focus-flash'),1200)}});$('provenanceList').setAttribute('title','근거 문장을 누르면 편집기에서 찾아갑니다.')}
  function versionCompare(){const controls=q('.version-controls');const button=make('button',{type:'button',class:'secondary',id:'compareVersion'},'현재 본문과 비교');const result=make('pre',{id:'versionDiff',class:'thread-card',hidden:''});controls.append(button);controls.insertAdjacentElement('afterend',result);button.addEventListener('click',()=>{const index=Number($('draftVersion').value);let versions=[];try{versions=JSON.parse(localStorage.getItem('heunjeok-naver-versions')||'[]')}catch{}const old=versions[index]?.draft;if(typeof old!=='string'){result.hidden=false;result.textContent='비교할 버전을 먼저 선택해 주세요.';return}const current=$('draft').value;const oldLines=new Set(old.split('\n'));const currentLines=new Set(current.split('\n'));const removed=old.split('\n').filter(v=>v&&!currentLines.has(v)).map(v=>`− ${v}`);const added=current.split('\n').filter(v=>v&&!oldLines.has(v)).map(v=>`+ ${v}`);result.textContent=[...removed,...added].slice(0,80).join('\n')||'문장 단위 차이가 없습니다.';result.hidden=false})}
  function staleDetails(){const capture=()=>({topic:$('selectedTopicStatus').textContent,type:$('postType').value,relationship:$('relationship').value,answers:essentialFields().map(v=>v.value),memo:$('memo').value,photos:$('photoNotes').value});$('generate').addEventListener('click',()=>{lastInputSnapshot=JSON.stringify(capture())});const note=make('span',{class:'storage-note',id:'staleDiff'});const staleNotice=$('staleNotice');staleNotice.append(note);const observer=new MutationObserver(()=>{if(!staleNotice.isConnected||staleNotice.hidden||!lastInputSnapshot)return;const before=JSON.parse(lastInputSnapshot),now=capture(),changed=[];if(before.topic!==now.topic)changed.push('주제');if(before.type!==now.type)changed.push('글 종류');if(before.relationship!==now.relationship)changed.push('광고 관계');if(JSON.stringify(before.answers)!==JSON.stringify(now.answers))changed.push('경험 답변');if(before.memo!==now.memo)changed.push('메모');if(before.photos!==now.photos)changed.push('사진 설명');note.textContent=changed.length?` 변경: ${changed.join(' · ')}`:''});observer.observe(staleNotice,{attributes:true,attributeFilter:['hidden']})}
  function benchmarkGuard(){const reference=$('benchmarkReference'),run=$('benchmarkRun');const label=make('label',{class:'storage-note'});const check=make('input',{type:'checkbox',id:'benchmarkRights'});label.append(check,d.createTextNode(' 내 글 또는 구조를 비교할 권리가 있는 글만 붙여넣었습니다.'));reference.insertAdjacentElement('afterend',label);run.disabled=true;check.addEventListener('change',()=>run.disabled=!check.checked);const notice=make('p',{class:'storage-note'},'구조 유사도는 문단·소제목 배치를 비교할 뿐 조회수·노출을 예측하지 않습니다. 결과에서는 우선 고칠 3가지만 먼저 보세요.');run.parentElement.insertAdjacentElement('afterend',notice);run.addEventListener('click',()=>setTimeout(()=>{const gaps=$('benchmarkGaps');[...gaps.children].slice(3).forEach(item=>item.hidden=true);if(gaps.children.length)gaps.insertAdjacentElement('beforebegin',make('strong',{},'우선 고칠 3가지'))},0))}
  function commentTools(){const input=$('commentInput');const host=input.parentElement;const chips=make('div',{class:'platform-switch','aria-label':'답변 말투'});let tone='polite',previous='';[['polite','공손'],['friendly','친근'],['short','짧게']].forEach(([value,label],i)=>{const b=make('button',{type:'button','aria-pressed':String(i===0)},label);b.addEventListener('click',()=>{tone=value;qa('.comment-workspace .platform-switch button').forEach(x=>x.setAttribute('aria-pressed',String(x===b)))});chips.append(b)});input.insertAdjacentElement('afterend',chips);const diff=make('p',{class:'storage-note','aria-live':'polite'});chips.insertAdjacentElement('afterend',diff);input.addEventListener('focus',()=>previous=input.value);input.addEventListener('input',()=>{if(previous&&previous!==input.value)diff.textContent=`원 댓글이 바뀌었습니다: ${Math.abs(input.value.length-previous.length)}자 차이. 답변을 다시 만들어 주세요.`});$('makeReplies').addEventListener('click',()=>setTimeout(()=>qa('#replyList textarea').forEach(area=>{if(tone==='short')area.value=area.value.split(/(?<=[.!?])\s+/).slice(0,1).join(' ');if(tone==='polite')area.value=area.value.replace(/해요/g,'합니다').replace(/이에요/g,'입니다')}),0))}
  function generationState(){const button=$('generate');const cancel=make('button',{type:'button',class:'text-button',id:'cancelGenerate',hidden:''},'생성 취소');const skeleton=make('div',{class:'draft-skeleton',id:'draftSkeleton',hidden:'','aria-label':'초안 만드는 중'});for(let i=0;i<4;i++)skeleton.append(make('span'));$('emptyResult').insertAdjacentElement('afterend',skeleton);button.insertAdjacentElement('afterend',cancel);let ticket=0;button.addEventListener('click',()=>{if(button.disabled)return;ticket++;const mine=ticket;button.setAttribute('aria-busy','true');cancel.hidden=false;skeleton.hidden=false;setTimeout(()=>{if(ticket===mine){button.setAttribute('aria-busy','false');cancel.hidden=true;skeleton.hidden=true}},600)});cancel.addEventListener('click',()=>{ticket++;button.setAttribute('aria-busy','false');cancel.hidden=true;skeleton.hidden=true;showCopyStatus('화면의 생성 표시를 중단했습니다. 이미 만든 로컬 초안은 그대로 둡니다.',false)})}
  async function photoCaptions(){const Photos=globalThis.HeunjeokPhotos;if(!Photos?.updatePhotoNote)return;const list=$('photoVaultList');async function render(){let photos=[];try{photos=await Photos.listPhotos()}catch{return}[...list.children].forEach((item,index)=>{if(item.querySelector('.photo-caption'))return;const photo=photos[index];if(!photo)return;const wrap=make('label',{class:'photo-caption'},`${photo.fileName||`${index+1}번째 사진`} 설명·alt 초안`);const input=make('input',{placeholder:'사진에서 실제로 보이는 것만 적어주세요.'});input.value=photo.note||'';let timer;input.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(()=>Photos.updatePhotoNote(photo.id,input.value),400)});wrap.append(input);item.append(wrap)})}new MutationObserver(()=>render()).observe(list,{childList:true});$('photoFiles').addEventListener('change',()=>{$('photoVaultStatus').textContent='사진을 축소하고 위치정보를 제거하는 중…'});await render()}
  function assembleChoices(){const host=$('assembleResult');new MutationObserver(()=>{const primary=host.querySelector('.improve-controls .primary');if(!primary||host.querySelector('#replaceWithSkeleton'))return;primary.textContent='뼈대만 아래에 병합';const replace=make('button',{type:'button',class:'secondary',id:'replaceWithSkeleton'},'현재 본문을 뼈대로 교체');replace.addEventListener('click',()=>{if(!confirm('현재 본문을 4단계 뼈대로 바꿀까요? 기존 본문은 버전 기록에서 복구할 수 있습니다.'))return;primary.click();const draft=$('draft');const marker=draft.value.lastIndexOf('# 4단계 뼈대');if(marker>=0){draft.value=draft.value.slice(marker);draft.dispatchEvent(new Event('input',{bubbles:true}))}});primary.insertAdjacentElement('afterend',replace)}).observe(host,{childList:true,subtree:true})}
  function avoidHighlights(){const draft=$('draft');const box=make('div',{class:'hashtag-editor','aria-live':'polite'});q('.editor-meta').insertAdjacentElement('afterend',box);function update(){let tone={};try{tone=JSON.parse(localStorage.getItem('heunjeok-naver-tone')||'{}')}catch{}const hits=(tone.avoidPhrases||[]).filter(value=>value&&draft.value.includes(value));box.replaceChildren();if(hits.length)box.append(make('strong',{},'쓰지 않기로 한 표현: '));hits.forEach(value=>box.append(make('span',{class:'hashtag-chip'},value)))}draft.addEventListener('input',update);update()}
  function researchDisclosures(){qa('#live-research .live-category-grid a').forEach(link=>link.append(make('small',{},'조회수 상위 아님 · 네이버 검색 열기')));const query=$('similarQuery');if(query)query.placeholder='직접 검색할 네이버 블로그 주제';q('.live-disclosure').textContent='링크마다 조회수 상위가 아님을 표시합니다 · 외부 글 저장 없음'}
  function dataTimes(){const list=$('dataInventory');function decorate(){[...list.children].forEach(item=>{if(item.querySelector('.data-time'))return;const title=item.querySelector('strong')?.textContent||'';const map={'경험 입력':'heunjeok-naver-workspace','말투 카드':'heunjeok-naver-tone','초안':'heunjeok-naver-package','본문 버전':'heunjeok-naver-versions'};const raw=localStorage.getItem(map[title]);if(!raw)return;let date='';try{const value=JSON.parse(raw);date=value.savedAt||value.createdAt||value.updatedAt||((Array.isArray(value)&&value.at(-1)?.savedAt)||'')}catch{}const span=make('span',{class:'data-time'},date?`마지막 수정 ${new Date(date).toLocaleString('ko-KR')}`:'수정 시각 정보 없음');item.querySelector('div')?.append(span)})}new MutationObserver(decorate).observe(list,{childList:true});decorate()}
  function inputSaveSticky(){const state=$('workspaceSaveState');state.parentElement.classList.add('sticky-save-state');const observer=new MutationObserver(()=>{if(/저장됨/.test(state.textContent)&&!/방금/.test(state.textContent))state.textContent+=' · 방금'});observer.observe(state,{childList:true,characterData:true,subtree:true})}
  function toneDetails(){const host=$('toneProfile');function render(){let details=$('toneDetails');if(!details){details=make('p',{class:'storage-note',id:'toneDetails'},'원문은 저장하지 않고 특징 카드만 저장합니다.');host.append(details)}let tone={};try{tone=JSON.parse(localStorage.getItem('heunjeok-naver-tone')||'{}')}catch{}if(!tone.averageSentenceChars)return;const counts=tone.endingCounts||{};details.textContent=`평균 문장 ${tone.averageSentenceChars}자 · 말끝 부드러움 ${counts.soft||0}회 / 정중함 ${counts.formal||0}회 / 반말 ${counts.casual||0}회 · 연결어 ${(tone.connectors||[]).join(' · ')||'없음'} · 원문 비저장`}render();$('saveTone').addEventListener('click',()=>setTimeout(render,0))}
  function emptyAction(){const empty=$('emptyResult');const button=make('button',{type:'button',class:'primary',id:'emptyNext'},'주제 고르기');empty.append(button);function update(){const ready=topicChosen();button.textContent=ready?'경험 세 칸 답하기':'주제 고르기';button.onclick=()=>$(ready?'experience-tool':'topicSection').scrollIntoView({behavior:'smooth',block:'start'})}new MutationObserver(update).observe($('selectedTopicStatus'),{childList:true,characterData:true,subtree:true});update()}
  function init(){runtimeBanner();position();quickRoute();simplifyFlow();wizard();mobileTabs();advanced();interviewTools();toneTools();toneDetails();editor();emptyAction();naverFinishPanel();hashtagEditor();avoidHighlights();copyStatus();quality();provenanceLinks();versionCompare();staleDetails();benchmarkGuard();commentTools();generationState();photoCaptions();assembleChoices();researchDisclosures();backup();dataTimes();inputSaveSticky();theme();footer();onboarding();qa('input,textarea,select').forEach(el=>el.addEventListener('input',progress));progress()}
  init();
})();
